/*
* @Author: 100yang
* @Date:   2019-10-28
* @Last Modified by:   100yang
* @Last Modified time: 2019-10-28
*/

#include <bits/stdc++.h>
using namespace std;
class Solution {
public:
	int threeSumClosest(vector<int>& nums, int target) {
		int n = nums.size();
		sort(nums.begin(), nums.end());
		for (int ;;) {}
	}
} mm;
int main() {
	std::vector<int> v;
	int n, a, tar;
	cin >> n >> tar;
	while (n--) {cin >> a; v.push_back(a);}
	cout << mm.threeSumClosest(v, tar) << endl;
	return 0;
}